
# Things i want to remember for the right time

## So Next ToDo's

- [ ] **Ingestion** - fix the pipeline. 
- [ ] **Story Clustering** - Fix the Issues with story clustering. 
- [ ] **Admin Dashboard** - create it first then -
- [ ] **Frontend** - Update the frontend. fix the small issues. Improve the ui. Ensure better discovery.
- [ ] **Locked Topics** - After all these put time in locked topics. after you introduce this to everyone. 

## Random things - Jot downs
- How well the gazetteer is now? 
- If `export const primaryConfig = {}` is used in other files too, it should not be in the `client.js` file. it should be in a generic file. 
- 


## Locked topics
- Remember to update the `locaked-topics` doc for up to date info directly checking the whole locked topics implementation. 
	- After all the changes ingestion pipeline has gone through i should check the locked topics implementation too, to see whether there's a part which i can do better. Maybe implement a ML based something Or Overall combination of the scanners, making them more robust and etc. Or maybe ideate a little to find a better way or a new feature. 
	- **Scraper fragility** - i should get a notification for it - when scraper can't succeed. A note for the notification feature. 
	- For the batching notification for size concerns - i may use a filtering pattern based on the user's concern or use a synthesizing option. 

## On AI Models
I should find do a proper audit again as i'm adding mistral to the ai provider list. already added the api key as `MISTRAL_API_KEY`.


## Story
The story creation and auto archiving feature needs auditing. check [Story clustering process evaluation](Story%20clustering%20process%20evaluation.md). This serves the whole audit. But the one i'm noting here is the env label clustering lifecycle. 
```shell
# Cluster lifespan
CLUSTER_STABLE_INACTIVE_DAYS=7
CLUSTER_LOW_IMPACT_INACTIVE_DAYS=10
CLUSTER_MEDIUM_IMPACT_INACTIVE_DAYS=21
CLUSTER_HIGH_IMPACT_INACTIVE_DAYS=35
CLUSTER_CRITICAL_IMPACT_INACTIVE_DAYS=60
```

Should we have these or not based on the system we're progressing. can we handle that any better way? or this is the best? 

- Currently clustering doesn't have any fallback models configured. I should add one. 
  As thinking of adding mistral they have generous limits. add a fallback from theirs. 
- Why clustering is tied down to  `client.js` and `processor.js`. is it has to be or just for the convenience? if doable it should completely be a separate feature with its isolated code. 
  as i guess runCluster does the orchestrating jobs. 
  


