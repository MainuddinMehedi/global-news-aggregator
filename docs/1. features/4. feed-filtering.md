# Feed, Pagination, & 3-Axis Filtering

> **Feature Status:** Implemented  
> **Feature ID:** `FEED_FILTERING`

---

## 🔗 Related Logs & References Map

- *No related research logs currently exist. This document serves as the primary technical layout for this feature.*

---

### 📖 Table of Contents
- [[#1. Overview & Objective]]
- [[#3. Research & Decision Matrix]]
- [[#4. Technical Architecture & Data Model]]
- [[#5. Implementation Notes & Reference Guide]]
- [[#6. Future Roadmap & "Remember It" Notes]]
- [[#7. Frontend Refactoring Guidelines (Staged)]]
- [[#8. Brainstorming & Feature Ideas]]

---

## 1. Overview & Objective

The **Infinite Feed & Analytical Filters** form the core user interface of the Global News Aggregator. Instead of simple category buttons, the feed implements a robust, state-preserving infinite scroll with specialized 3-axis filters designed to track geopolitical bias, country perspectives, and regional news dynamics.

### The 3-Axis Geopolitical Filter Architecture
To allow deep analysis of narratives, news is classified along three distinct axes:

1. **`Event Region` (What is the news about?):** The geographical subject of the article (e.g., Middle East, Asia-Pacific). Extracted dynamically via Stage 1 Gazetteer or AI.
2. **`Source Origin` (Where is the publisher based?):** The macro region / geopolitical base of the news outlet (e.g., Russia is Europe, Al Jazeera is Middle East). *Note: Derived dynamically from sourceCountry.*
3. **`Source Type` (What kind of publisher is it?):** The editorial structure (e.g., *State Media, Independent Wire, Commercial Publisher, Amplifier/Aggregator*).

*Example Use Case:* An analyst can filter the feed to show articles covering the **Middle East** (`Event Region`) reported by **State Media** (`Source Type`) based in **Europe** (`Source Origin`), showing exact soft-power narrative angles.

### Scope Boundaries
- **Cursor-Based Infinite Scroll:** Avoids offsets to prevent skipped or duplicated items when new articles are ingested in real-time.
- **URL-Driven Filters:** All navigation state (search, category, sorting) lives in the URL, enabling instant shareability and standard browser back/forward navigation.
---

## 3. Research & Decision Matrix

### Dimensional Falsification Matrix

| Candidate Solution | Efficacy & Determinism | Operational Cost / Latency | Complexity / Debt | Outcome / Verdict |
| :--- | :--- | :--- | :--- | :--- |
| **Option A: Offset-Based Pagination** (`LIMIT Y OFFSET X`) | ❌ **Low** (Row positions shift constantly as new RSS crawls occur, causing duplicates/skips in feed) |  **Low** (Simple DB query) |  **Low** (Standard framework setup) | **Disproven** (Unstable on real-time feeds) |
| **Option B: Cursor-Based Pagination** (`take: Y + 1, cursor: { id: lastSeenId }`) |  **High** (Completely stable. Points to a specific anchor ID and scrolls relative to it) |  **Low** (Saves SQL execution plans; doesn't scan entire tables) | ⚠️ **Medium** (Requires tracking tiebreakers if timestamps match) | **Accepted** (Current Implementation) |

---

## 4. Technical Architecture & Data Model

The pagination architecture operates via hybrid React Server Components (RSC) and Client Components to deliver instant First Contentful Paint (FCP) while maintaining smooth infinite scroll.

```
layout.tsx (Static HTML shell)
  └── <Suspense fallback={<FeedSkeleton />}>
        page.tsx (Reads searchParams at request time, queries first 20 articles)
          └── ArticleFeed.tsx (Client component; mounts initial 20 articles, listens to scroll)
                ├── IntersectionObserver (Sentinels trigger fetch page + 1)
                └── URL Query Syncer (Sorts, filters, search writes)
```

### Pagination Implementation details
- **Take + 1 Trick:** We fetch `TAKE + 1` (21) rows. If 21 items return, we know a next page exists. The `nextCursor` is set to the 20th item's ID. If 20 or fewer return, `nextCursor` becomes `null`.
- **Unique Tiebreaker:** Prisma query ordering always appends `{ id: "asc" }` to prevent paging ambiguity if multiple articles share an identical `publishedAt` timestamp.

---

## 5. Implementation Notes & Reference Guide

### Key Code Artifacts
- [page.tsx](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/frontend/app/page.tsx) — Main feed page fetching initial SSR data.
- [articles.ts](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/frontend/queries/articles.ts) — Houses `getArticles()` query wrapped in Next.js `'use cache'`.
- [ArticleFeed.tsx](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/frontend/components/Feed/ArticleFeed.tsx) — Manages infinite scrolling, sentinels, and client state.
- [route.ts](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/frontend/app/api/articles/route.ts) — GET paginated endpoint used by the client for infinite scroll loading.

### Key Execution Commands
- **Start Next.js Frontend Dev:** `cd frontend && npm run dev`
- **Build Frontend Bundle:** `cd frontend && npm run build`

### Responsive Layout Grid (Breakpoints)
- **`< md` (< 768px):** Mobile sidebar is hidden (accessible via Sheet drawer). Feed takes full width.
- **`md → lg` (768px - 1024px):** Sidebar is icon-only (56px). Feed occupies remaining width.
- **`lg → xl` (1024px - 1280px):** Sidebar is full size (224px).
- **`xl+` (≥ 1280px):** Sidebar is full size. Left feed occupies center, and the right panel (288px) displays Analytics widgets.

---

## 6. Future Roadmap & "Remember It" Notes

- **Full-Text Search Scaling:** The current search applies `ILIKE '%query%'` across fields. This cannot use B-tree indexes. If performance degrades under volume, upgrade to PostgreSQL Full-Text Search (`tsvector` + GIN Index) in Supabase or connect Algolia.
- **Zustand State Syncer:** Live feed counts are managed by the Zustand `FeedSlice`. Ensure that whenever a filter remount occurs, selectors are updated cleanly.

---

## 7. Frontend Refactoring Guidelines (Staged)

*Note: Since work is currently focused strictly on the backend and database, these guidelines document the upcoming changes required on the frontend once you return to refine its layout.*

### Replacing `sourceOrigin` with Dynamic Lookup
Because `sourceOrigin` was dropped from `schema.prisma`, any query logic or widget rendering that checked `rawArticle.sourceOrigin` must be updated:

1. **Mapping Region on the fly in `PerspectiveWidget`:**
   - Grouping articles by macro origin (e.g. Europe vs Middle East) must import `getPublisherRegion(country)` and call it with the article's `sourceCountry` string.
2. **Dynamic In-Memory Aggregations (`queries/analytics.ts`):**
   - Refactor `rawArticle.groupBy({ by: ['sourceOrigin'] })` to group by `sourceCountry` instead.
   - On retrieve, sum country tallies into their macro regions in memory to build the slices of the bias donut charts.
3. **Filtering by Region (`queries/articles.ts`):**
   - Instead of checking `sourceOrigin: origin` directly in Prisma queries, lookup the matching countries for that region (e.g., `"Middle East"` -> `["Qatar", "Saudi Arabia"]`) and run a Prisma `in` comparison:
     ```typescript
     if (origin) {
       const countries = getCountriesInRegion(origin); // Helper mapping
       originFilter = [{ rawArticle: { sourceCountry: { in: countries } } }];
     }
     ```

---

## 8. Brainstorming & Feature Ideas

- **Custom Analytical Saved Views:** Allow authenticated users to save their specific 3-axis combination configurations as a "Workspace Shortcut" (e.g., "State Media Watch" or "Sub-Saharan Africa Tracker").
- **Interactive Multi-Axis Donut Charts:** An analytics dashboard widget where clicking sectors automatically builds the corresponding 3-axis query parameters and redirects the user to the filtered feed.
