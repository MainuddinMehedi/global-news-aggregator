# Story Clustering

> **Feature Status:** Implemented (Maintenance & Evolution)  
> **Feature ID:** `STORY_CLUSTERING`

---

## 📖 References Map

_(Items that require quick checkup, future plans, or jotting down)_

- None currently active.

> [!NOTE]
> Phase 3 (Vectorization & Deduplication) has been implemented to resolve O(N²) scaling, LLM prompt token bloat, centroid drift, and stable lifecycle deactivation limits.
> 
> Remaining risks / tasks:
> - No clustering observability dashboard yet (to visually audit matches/misses).
> - No mature entity canonicalization table (currently maps via local string rules).
> - Article-level themes / event tags (currently uses category and text overlap fallback).
> - `storyType` Field Separation: Separating `BREAKING`, `SLOW_BURN` out of the narrative phase statuses.


## 🎯 Current Direction
_(Always keep this section up-to-date. This outlines the current intentions and what we are actively trying to achieve with this feature. Before doing any tasks, we align with this intention.)_

Following the implementation of Phase 3, the current focus is on verification and monitoring:
1. Validating the efficacy of the `pgvector` distance thresholds (`0.25` for pre-clustering, `0.38` for fast-exit, `0.15` for post-run medoid deduplication).
2. Monitoring whether the dynamic, impact-scaled lifecycle limits (7, 14, 21, 30 days for stable/resolving clusters) prevent premature archiving of high-impact stories.

👉 **[Immediate Action Board](action-board.md)**

---

## ⏳ Explicitly Deferred (Future Notes)
_(Items that are good ideas but not required right now. When the Immediate Action Board is empty, these items move up into the Current Direction.)_

- **Hidden Candidate Stories:** Single-article detection with candidate-to-story promotion.
- **Article-level themes:** Adding themes to articles for better candidate scoring.
- **`storyType` Field Separation:** Separating `BREAKING`, `SLOW_BURN` out of the narrative phase statuses.

---
---

## 📚 Technical Overview & Deep Dive
_(This section contains heavy schemas, architecture notes, and deep technical overviews. It is pushed to the bottom so it doesn't clutter the immediate action workflow. Update this occasionally when breaking changes or major architectural shifts occur.)_

### Table of Contents
- [[#1. Overview & Objective]]
- [[#2. Research & Architectural Decisions]]
- [[#3. Research & Decision Matrix]]
- [[#4. Technical Architecture & Data Model]]
- [[#5. Implementation Notes & Reference Guide]]
- [[#6. Brainstorming & Feature Ideas]]

---

## 2. Research & Architectural Decisions
_(The "Why" behind the system. Read these if you need to understand why certain paths were chosen or deferred.)_

- **[Story Clustering: Core Reasoning & Shifts](story-core-reasoning.md)** — *A synthesized 2-minute read capturing the core product philosophy, the key architectural shifts from the MVP, and the Phase 2 roadmap.*

---

## 1. Overview & Objective

The **Story Clustering** feature acts as the "Global Billboard" of the platform. Instead of presenting a raw, unfiltered firehose of individual news articles, it groups redundant, overlapping, or related reports from different publishers into cohesive, evolving geopolitical narratives called **Stories**.

### Why It Exists
- **Information Overload:** A major event (e.g., peace talks, a natural disaster, or geopolitical skirmishes) triggers dozens of articles from different outlets, cluttering the feed.
- **Perspective Highlight:** Grouping articles into clusters allows the frontend to contrast coverages, showing the "Perspective Gap" (e.g., state media vs. commercial media).
- **Token Efficiency:** Raw news is ingested constantly. By grouping articles locally before hitting an LLM, the system processes groups in batches, protecting against rate limits.

### Scope Boundaries
- **Dynamic Cluster Lifecycle:** Stories dynamically emerge, transition through narrative phases, and auto-archive based on impact + status + inactivity rules.
- **Frontend Top 30 Cap:** The frontend shows the top 30 active stories by a composite rank score (impact × recency × source diversity × article count × momentum). Stories beyond 30 remain active in the DB for lifecycle-appropriate retention.
- **LLM Relevant Context:** The LLM receives only the top relevant candidate stories per article batch (not the global top 30), selected by entity/region/category/text overlap scoring.

---

## 3. Research & Decision Matrix

### Dimensional Falsification Matrix

| Candidate Solution | Efficacy & Determinism | Operational Cost / Latency | Complexity / Debt | Outcome / Verdict |
| :--- | :--- | :--- | :--- | :--- |
| **Option A: Ingestion-Time LLM Clustering** (Clustering every article immediately using LLM) | ❌ **Low** (Highly prone to premature categorization, duplicate story creation, and context limit fatigue) | ❌ **High** (Constant API requests to LLM, high token cost) | ⚠️ **Medium** | **Disproven** (Unpredictable & costly) |
| **Option B: Pure Local TF-IDF / NLP Clustering** (Group purely using offline word similarities) | ❌ **Medium** (High speed but misses semantic nuances; hard to extract high-quality titles/summaries without LLM) |  **Low** (Completely offline, near zero cost) | ⚠️ **Medium** (Requires tweaking NLP thresholds constantly) | **Disproven** (Fails to create high-quality readable summaries) |
| **Option C: Decoupled Entity-Matching + LLM Batching** (Deterministic local entity filtering first, followed by batch LLM decisions) |  **High** (Entity matching groups candidate articles locally. LLM only performs final categorization and summaries) |  **Medium** (LLM is called in batches only when critical mass is met; rate limits are respected) |  **Low** (Clean separation of concerns, stable) | **Accepted** (Current Implementation) |

---

## 4. Technical Architecture & Data Model

To prevent API rate limits, network crashes, and LLM amnesia, the system uses a **Decoupled Architecture**: Ingestion and Clustering run as asynchronous, independent processes.

### Data Model (Prisma Schema)

_Current as of Phase 1. See [schema.prisma](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/prisma/schema.prisma) for the authoritative definition._

```prisma
model ProcessedArticle {
  id             String         @id @default(uuid())
  rawArticleId   String         @unique
  rawArticle     RawArticle     @relation(fields: [rawArticleId], references: [id], onDelete: Cascade)
  categories     Category[]     @relation("ProcessedArticleCategories")
  entities       String[]
  sentimentScore Float?
  eventRegion    String?
  clusterStatus  String         @default("HOLDING") // HOLDING | CLUSTERED | ARCHIVED_UNCLUSTERED
  clusteredAt    DateTime?
  storyClusters  StoryCluster[] @relation("ArticleStoryClusters")
}

model StoryCluster {
  id               String   @id @default(uuid())
  slug             String   @unique
  title            String
  summary          String   @db.Text
  timeWindow       String?
  keyDevelopments  Json?
  isActive         Boolean  @default(true)
  impact           String?  // CRITICAL | HIGH | MEDIUM | LOW
  impactScore      Float?
  status           String?  @default("DEVELOPING") // EMERGING | DEVELOPING | ESCALATING | SLOW_BURN | STABLE | RESOLVING | ARCHIVED
  momentumScore    Float    @default(0)
  whyItMatters     String?  @db.Text
  regions          String[]
  themes           String[]
  articleCount     Int      @default(0)
  sourceCount      Int?
  topSources       String[]
  lastActivityAt   DateTime?
  activityScore    Float?
  trendData        Json?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  articles         ProcessedArticle[] @relation("ArticleStoryClusters")
}
```

### Process Flow
1. **Ingestion & Holding Tank:** `npm run ingest` crawls new articles, runs AI enrichment (entity extraction, categorization, sentiment), and writes them as `HOLDING`.
2. **Lifecycle Check:** The worker (`npm run cluster`) first applies lifecycle rules — active stories that have exceeded their inactivity window (based on impact × status) are deactivated.
3. **Holding Window Check:** `HOLDING` articles within the configurable accumulation window (default 168h, env `CLUSTER_HOLDING_WINDOW_HOURS`) are fetched. Older articles are silently archived as `ARCHIVED_UNCLUSTERED`.
4. **Pre-Clustering Adjacency:** Articles are grouped using connected-components. For articles with embeddings, pairwise connections are established in the database using raw SQL cosine distance (`(a1.embedding <=> a2.embedding) < CLUSTER_PRE_CLUSTERING_THRESHOLD`, default `0.25`). Articles without embeddings use a legacy fallback: they are linked if they share $\ge \text{CLUSTER\_MIN\_ENTITY\_OVERLAP}$ (default `2`) normalized entities.
5. **Critical Mass Check:** Connected components with at least **3 articles** (env `CLUSTER_MIN_GROUP_SIZE`) proceed to AI enrichment. Others remain in `HOLDING`.
6. **Token-Safe Chunking:** Grouped articles are sliced into batches of 5.
7. **Relevant Candidate Selection & Fast-Exit:** For each batch, the nearest active clusters are queried using a raw SQL vector distance query on the closest article embedding (`MIN(p.embedding <=> a.embedding)`). Active clusters with zero article embeddings (legacy clusters) are scored in-memory using the entity/text overlap heuristic and filtered (`CLUSTER_LEGACY_MIN_SCORE`, default `10.0`). If the nearest vector distance $> \text{CLUSTER\_FAST\_EXIT\_THRESHOLD}$ (default `0.38`) and no legacy match is found, **Fast-Exit** triggers, returning zero candidates to avoid injecting irrelevant prompt context. Otherwise, the candidates are merged and sorted by similarity.
8. **LLM Categorization:** Groq (or fallback model) receives the batch articles + relevant candidate stories (unless fast-exit triggered). It decides assignment, new cluster creation, or unclustered status.
9. **Database Write:** Results are persisted — new clusters are created with on-the-fly medoid assignments, and existing clusters are updated. Unassigned articles are marked `ARCHIVED_UNCLUSTERED`.
10. **Post-Run Deduplication & Lifecycle decay:**
    - **Medoid-Based Deduplication:** Active clusters are compared using the cosine distance of their medoids (computed on-the-fly using the last 20 articles). Pairs with a distance $< \text{CLUSTER\_DEDUPLICATION\_THRESHOLD}$ (default `0.15`) are merged in a Prisma transaction, deactivating the newer cluster and re-routing articles.
    - **Momentum Decay:** Active cluster momentum decays globally by 0.5.
    - **Cache Revalidation:** After the run completes (and if any duplicate merges occurred), `revalidateCache(["articles", "stories"])` is triggered.

```mermaid
graph TD
    A[RSS Feeds Ingestion] -->|AI Enrichment| B[ProcessedArticle: HOLDING]
    B -->|Lifecycle Check| L{Deactivate Stale?}
    L -->|Impact-Scaled Cutoff| M[Deactivate Story]
    B -->|Holding Window Pass| N{Within Window?}
    N -->|No (>168h default)| O[ARCHIVED_UNCLUSTERED]
    N -->|Yes| C{Pre-Clustering?}
    C -->|Vector Distance <0.25 / Entity Overlap| E[Batch into Chunks of 5]
    C -->|No Overlap / Mass <3| D[Stay in HOLDING]
    E -->|Raw SQL Candidate Selection| F{Fast-Exit? Dist >0.38}
    F -->|Yes| G[Skip LLM Context]
    F -->|No| H[Groq AI Decision]
    G -->|Create New Story| I[StoryCluster Updated]
    H -->|Assign / Create Story| I
    I -->|Post-run Medoid Dedup <0.15| J[Merge Duplicates]
    J -->|Momentum Decay + Revalidation| K[Next.js Frontend Refreshed]
```

---

## 5. Implementation Notes & Reference Guide

### Key Code Artifacts
- [runClustering.js](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/ingestion-service/runClustering.js) — Main orchestrator: lifecycle check, holding window, pre-clustering grouping, candidate selection, LLM processing, post-run deduplication.
- [clustering/utils/overlap.js](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/ingestion-service/clustering/utils/overlap.js) — Pre-clustering entity & vector-distance adjacency builder.
- [clustering/utils/relevance.js](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/ingestion-service/clustering/utils/relevance.js) — Raw SQL active candidate vector search and legacy fallback with Fast-Exit check.
- [clustering/dedup.js](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/ingestion-service/clustering/dedup.js) — Post-run cluster medoid deduplication.
- [clustering/ai.js](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/ingestion-service/clustering/ai.js) — Prompt builder for LLM clustering decisions.
- [clustering/lifecycle.js](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/ingestion-service/clustering/lifecycle.js) — Dynamic, impact-scaled cluster state transitions (stable cutoff scaled by impact: 7, 14, 21, 30 days).
- [schema.prisma](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/prisma/schema.prisma) — Database schema definition.
- [frontend/queries/stories.ts](file:///home/mainu/programming/projects/automation/geopolitical-news-monitor/global-news-aggregator/frontend/queries/stories.ts) — Frontend story query with composite rank scoring (impact × recency × article count × source diversity × momentum).

### Key Execution Commands
- **Run Ingestion:** `npm run ingest` (Run first to seed raw inputs).
- **Run Clustering Worker:** `npm run cluster` (Executes the clustering logic on `HOLDING` articles).

---



## 7. Brainstorming & Feature Ideas

- **Side-by-Side Coverage Compare:** Provide a UI view where users click a Story and compare the news coverage of state media vs. commercial outlets side-by-side on a timeline.
- **Multi-lingual Cluster Detection:** Enable the python microservice to translate/normalize entities across different languages so foreign sources can easily cluster with domestic articles.
- **Interactive Story Timelines:** Generate milestone timelines automatically from the chronological developments of a cluster.

---

## 🔗 Related Logs
_(Historical decisions, audits, and performance metrics that do not require quick checkups)_

- **[Story DB Baseline Audit](../../archive_dump/story-db-baseline-audit.md)** — *Baseline DB audit script and tracking fields for measuring Phase 1 impact. Requires live DB connection.*
- **[STORY_FEATURE](../../archive_dump/STORY_FEATURE.md)** — *High-level overview of the evolving story clustering logic.*
- **[STORIES_ARCHITECTURE](../../archive_dump/STORIES_ARCHITECTURE.md)** — *Technical architecture details of the clustering worker.*
- **[Story clustering process evaluation](../../2.%20my_notes_&_drafts/Story%20clustering%20process%20evaluation.md)** — *Detailed analysis log of clustering thresholds, duplicate groupings, and decay parameters.*
- **[clustering_foundation_fixes](../../2.%20my_notes_&_drafts/clustering_foundation_fixes.md)** — *Developer log documenting the decoupling of the clustering pipeline from ingestion.*
